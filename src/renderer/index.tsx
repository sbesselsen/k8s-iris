import * as React from "react";
import * as ReactDOM from "react-dom";

ReactDOM.render(
    <React.StrictMode>
        <div>Renderer</div>
    </React.StrictMode>,
    document.getElementById("root")
);
